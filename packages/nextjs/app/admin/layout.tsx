import { AdminLayout } from "../../components/bluntdao-olympics/admin/layout/AdminLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <AdminLayout>{children}</AdminLayout>;
}
